# test-github-action

Testbed repo for [qiakai/x-cmd-action-ai](https://github.com/qiakai/x-cmd-action-ai)
GitHub Actions. The workflows under `.github/workflows/test-*.yml` exercise the
`commit`, `changelog`, `review`, `spec`, `translate`, and `triage` actions.

This file doubles as the translation test input (see `test-translate.yml`).
